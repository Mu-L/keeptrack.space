import { KeepTrackPlugin } from '@app/engine/plugins/base-plugin';

/**
 * OSS stub for the Stars plugin. Stars are a pro-only feature.
 */
export class StarsPlugin extends KeepTrackPlugin {
  readonly id = 'StarsPlugin';
}
